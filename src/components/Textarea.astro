---
const { id, label, rows = 4, placeholder = "", ...props } = Astro.props;
---
<div class="mb-4">
    <label for={id} class="block text-sm text-[#0A1748] font-bold mb-2">{label}</label>
    <textarea id={id} rows={rows} placeholder={placeholder} class="w-full p-2 rounded-2xl border-none focus:outline-none resize-none" style="box-shadow: inset 0 4px 8px rgba(0, 0, 0, 0.1);" {...props}></textarea>
</div>
