---
const { id, label, rows = 4, placeholder = "", ...props } = Astro.props;
---
<div class="mb-2">
    <label for={id} class="block text-sm text-[#0A1748] font-bold mb-2"><h5 class="mb-1 max-w-[350px]">{label}</h5></label>
    <textarea id={id} rows={rows} placeholder={placeholder} class="w-full md:w-[350px] p-2 border-none focus:outline-none resize-none" style="box-shadow: inset 0 4px 8px rgba(0, 0, 0, 0.1); border-radius: 35px;" {...props}></textarea>
</div>
